# Uma rede simples de bockchain, criada através da linguagem python.

libs usadas (Até o momento):

    - flask (Flask, jsonify)
    - haslib 
    - datetime

----------------------------------------------------------------------------------------------------

# A simple bockchain network, created using the python language.

used libs (so far):

    - flask (Flask, jsonify)
    - haslib 
    - datetime

